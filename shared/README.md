# Shared

Use this folder for code that should be reused by both `web/` and `mobile/`.

Examples for future additions:

- API client helpers
- Validation schemas
- Domain constants
- Shared utility functions
