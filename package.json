{
  "name": "claxi-repo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:web": "npm --prefix web run build",
    "dev:web": "npm --prefix web run dev",
    "test:functions": "npm --prefix functions test",
    "deploy:hosting": "npm run build:web && firebase deploy --only hosting:claxi",
    "deploy:functions": "firebase deploy --only functions",
    "deploy": "npm run build:web && firebase deploy --only functions,hosting:claxi"
  },
  "devDependencies": {
    "firebase-tools": "^14.19.1"
  }
}
