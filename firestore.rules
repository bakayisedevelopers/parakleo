rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Local emulator development only. Replace with least-privilege rules
    // before deploying rules to a real Firebase project.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
